# i18n 多言語対応 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** next-intl を使って RMTsite に英語（デフォルト）・日本語・中国語の3言語対応を追加し、ヘッダーに EN/JA/ZH 切り替えボタンを表示する。

**Architecture:** `app/[locale]/` ルートグループで URL ベースのロケール管理を行う。Server Components は `getTranslations()`（async）、Client Components は `useTranslations()` で翻訳を取得する。`middleware.ts` が `/` → `/en` のリダイレクトを担当し、`NextIntlClientProvider` が Client Components にメッセージを供給する。

**Tech Stack:** next-intl v3, Next.js 14 App Router, TypeScript

---

## ファイル構成（変更・作成一覧）

```
新規作成:
  middleware.ts
  i18n/routing.ts
  i18n/request.ts
  i18n/navigation.ts
  messages/en.json
  messages/ja.json
  messages/zh.json
  components/LanguageSwitcher.tsx
  __tests__/LanguageSwitcher.test.tsx

移動（内容も更新）:
  app/layout.tsx             → app/[locale]/layout.tsx
  app/page.tsx               → app/[locale]/page.tsx
  app/category/page.tsx      → app/[locale]/category/page.tsx

更新:
  next.config.mjs            （withNextIntl プラグイン追加）
  components/Header.tsx      （async、getTranslations、LanguageSwitcher 追加）
  components/Footer.tsx      （async、getTranslations）
  components/Carousel.tsx    （useTranslations）
  components/Sidebar.tsx     （async、getTranslations）
  components/GameRankingTable.tsx  （listingCount の件/listings 対応）
  __tests__/Header.test.tsx  （英語テキストに更新、next-intl モック）
  __tests__/Footer.test.tsx  （英語テキストに更新、next-intl モック）
  __tests__/Sidebar.test.tsx （英語テキストに更新、next-intl モック）
  __tests__/GameRankingTable.test.tsx （件 → listings に更新）
```

---

## Task 1: next-intl インストールと next.config.mjs 更新

**Files:**
- Modify: `next.config.mjs`

- [ ] **Step 1: next-intl をインストール**

```bash
cd C:/Users/unitu/RMTsite
npm install next-intl
```

期待出力: `added 1 package` のような成功メッセージ

- [ ] **Step 2: next.config.mjs を更新**

```javascript
// next.config.mjs
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./i18n/request.ts')

export default withNextIntl({})
```

- [ ] **Step 3: ビルドが通ることを確認（まだ i18n 設定がないので失敗は正常）**

```bash
npm run build 2>&1 | head -20
```

next-intl 関連の設定ファイルが足りないエラーが出るはず。それでOK（以降のタスクで解決する）。

- [ ] **Step 4: コミット**

```bash
git add next.config.mjs package.json package-lock.json
git commit -m "chore: next-intl をインストール"
```

---

## Task 2: i18n 設定ファイルを作成

**Files:**
- Create: `i18n/routing.ts`
- Create: `i18n/request.ts`
- Create: `i18n/navigation.ts`

- [ ] **Step 1: i18n/routing.ts を作成**

```typescript
// i18n/routing.ts
import { defineRouting } from 'next-intl/routing'

export const routing = defineRouting({
  locales: ['en', 'ja', 'zh'],
  defaultLocale: 'en',
  localeDetection: false
})
```

- [ ] **Step 2: i18n/request.ts を作成**

```typescript
// i18n/request.ts
import { getRequestConfig } from 'next-intl/server'
import { routing } from './routing'

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale
  if (!locale || !routing.locales.includes(locale as 'en' | 'ja' | 'zh')) {
    locale = routing.defaultLocale
  }
  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default
  }
})
```

- [ ] **Step 3: i18n/navigation.ts を作成**

```typescript
// i18n/navigation.ts
import { createNavigation } from 'next-intl/navigation'
import { routing } from './routing'

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing)
```

- [ ] **Step 4: コミット**

```bash
git add i18n/
git commit -m "feat: next-intl i18n 設定ファイルを追加"
```

---

## Task 3: 翻訳ファイルを作成（en / ja / zh）

**Files:**
- Create: `messages/en.json`
- Create: `messages/ja.json`
- Create: `messages/zh.json`

- [ ] **Step 1: messages/en.json を作成**

```json
{
  "header": {
    "search": "Search game titles",
    "login": "Login",
    "register": "Sign Up",
    "nav": {
      "pc": "PC Games",
      "mobile": "Mobile Games",
      "other": "Other",
      "all": "All Titles"
    }
  },
  "home": {
    "rankingTitle": "Popular Game Rankings",
    "mobileRanking": "Mobile Games Ranking",
    "pcRanking": "PC Games Ranking",
    "listingsTitle": "Latest Listings",
    "listingUnit": "listings"
  },
  "category": {
    "title": "Game Title List",
    "pc": "PC Games",
    "mobile": "Mobile Games",
    "other": "Other"
  },
  "sidebar": {
    "categoryTitle": "Categories",
    "searchTitle": "Search Titles",
    "searchPlaceholder": "Enter game name"
  },
  "footer": {
    "terms": "Terms of Service",
    "privacy": "Privacy Policy",
    "contact": "Contact Us",
    "guide": "User Guide"
  },
  "carousel": {
    "slide1": "24/7 Real-Time Trading Available",
    "slide2": "Beginner-Friendly Trading Guide",
    "slide3": "Multiple Payment Methods Supported"
  }
}
```

- [ ] **Step 2: messages/ja.json を作成**

```json
{
  "header": {
    "search": "ゲームタイトルを検索",
    "login": "ログイン",
    "register": "会員登録",
    "nav": {
      "pc": "PCゲーム",
      "mobile": "モバイルゲーム",
      "other": "その他",
      "all": "全タイトル一覧"
    }
  },
  "home": {
    "rankingTitle": "人気ゲームランキング",
    "mobileRanking": "モバイルゲーム 人気ランキング",
    "pcRanking": "PCゲーム 人気ランキング",
    "listingsTitle": "最新出品",
    "listingUnit": "件"
  },
  "category": {
    "title": "ゲームタイトル一覧",
    "pc": "PCゲーム",
    "mobile": "モバイルゲーム",
    "other": "その他"
  },
  "sidebar": {
    "categoryTitle": "カテゴリ",
    "searchTitle": "タイトル検索",
    "searchPlaceholder": "ゲーム名を入力"
  },
  "footer": {
    "terms": "利用規約",
    "privacy": "プライバシーポリシー",
    "contact": "お問い合わせ",
    "guide": "ご利用ガイド"
  },
  "carousel": {
    "slide1": "24時間365日リアルタイム取引対応",
    "slide2": "初心者向け取引ガイド完備",
    "slide3": "多様な決済方法に対応"
  }
}
```

- [ ] **Step 3: messages/zh.json を作成**

```json
{
  "header": {
    "search": "搜索游戏标题",
    "login": "登录",
    "register": "注册",
    "nav": {
      "pc": "PC游戏",
      "mobile": "手机游戏",
      "other": "其他",
      "all": "全部游戏"
    }
  },
  "home": {
    "rankingTitle": "热门游戏排行榜",
    "mobileRanking": "手机游戏排行榜",
    "pcRanking": "PC游戏排行榜",
    "listingsTitle": "最新出售",
    "listingUnit": "件"
  },
  "category": {
    "title": "游戏标题列表",
    "pc": "PC游戏",
    "mobile": "手机游戏",
    "other": "其他"
  },
  "sidebar": {
    "categoryTitle": "分类",
    "searchTitle": "搜索标题",
    "searchPlaceholder": "输入游戏名称"
  },
  "footer": {
    "terms": "服务条款",
    "privacy": "隐私政策",
    "contact": "联系我们",
    "guide": "使用指南"
  },
  "carousel": {
    "slide1": "全天候实时交易",
    "slide2": "新手友好交易指南",
    "slide3": "支持多种支付方式"
  }
}
```

- [ ] **Step 4: コミット**

```bash
git add messages/
git commit -m "feat: EN/JA/ZH 翻訳ファイルを追加"
```

---

## Task 4: middleware.ts を作成

**Files:**
- Create: `middleware.ts`

- [ ] **Step 1: middleware.ts を作成**

```typescript
// middleware.ts
import createMiddleware from 'next-intl/middleware'
import { routing } from './i18n/routing'

export default createMiddleware(routing)

export const config = {
  matcher: ['/((?!_next|_vercel|.*\\..*).*)']
}
```

- [ ] **Step 2: コミット**

```bash
git add middleware.ts
git commit -m "feat: next-intl middleware を追加（ロケールリダイレクト）"
```

---

## Task 5: app/ ディレクトリを app/[locale]/ に再構成

**Files:**
- Create: `app/[locale]/layout.tsx`（現 `app/layout.tsx` の内容を移動・更新）
- Create: `app/[locale]/page.tsx`（現 `app/page.tsx` を移動）
- Create: `app/[locale]/category/page.tsx`（現 `app/category/page.tsx` を移動）
- Delete: `app/layout.tsx`, `app/page.tsx`, `app/category/page.tsx`

- [ ] **Step 1: app/[locale]/ ディレクトリを作成して layout.tsx を作成**

```bash
mkdir -p "C:/Users/unitu/RMTsite/app/[locale]/category"
```

`app/[locale]/layout.tsx` を新規作成:

```typescript
// app/[locale]/layout.tsx
import type { Metadata } from 'next'
import '../globals.css'
import { NextIntlClientProvider } from 'next-intl'
import { getMessages } from 'next-intl/server'
import { notFound } from 'next/navigation'
import { routing } from '@/i18n/routing'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'RMTsite - Game Account & Item Trading',
  description: 'RMT (Real Money Trading) site for PC and mobile games',
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode
  params: Promise<{ locale: string }>
}) {
  const { locale } = await params
  if (!routing.locales.includes(locale as 'en' | 'ja' | 'zh')) {
    notFound()
  }
  const messages = await getMessages()

  return (
    <html lang={locale}>
      <body className="bg-white text-gray-800 min-h-screen flex flex-col">
        <NextIntlClientProvider messages={messages}>
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 2: app/[locale]/page.tsx を作成（現 app/page.tsx の内容をコピー）**

```typescript
// app/[locale]/page.tsx
import { getTranslations } from 'next-intl/server'
import Carousel from '@/components/Carousel'
import GameRankingTable from '@/components/GameRankingTable'
import ListingCard from '@/components/ListingCard'
import Sidebar from '@/components/Sidebar'
import gamesData from '@/data/games.json'
import listingsData from '@/data/listings.json'
import { Game } from '@/types'

export default async function HomePage() {
  const t = await getTranslations('home')
  const games = gamesData as Game[]

  const pcGames = games
    .filter((g) => g.category === 'pc')
    .sort((a, b) => b.listingCount - a.listingCount)
    .slice(0, 5)

  const mobileGames = games
    .filter((g) => g.category === 'mobile')
    .sort((a, b) => b.listingCount - a.listingCount)
    .slice(0, 5)

  const recentListings = listingsData.slice(0, 8)
  const listingUnit = t('listingUnit')

  return (
    <>
      <Carousel />
      <div className="max-w-screen-xl mx-auto px-4 py-6">
        <div className="flex gap-6">
          <div className="flex-1 min-w-0">
            <h2 className="text-base font-bold text-gray-700 mb-3">{t('rankingTitle')}</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
              <GameRankingTable games={mobileGames} title={t('mobileRanking')} listingUnit={listingUnit} />
              <GameRankingTable games={pcGames} title={t('pcRanking')} listingUnit={listingUnit} />
            </div>
            <h2 className="text-base font-bold text-gray-700 mb-3">{t('listingsTitle')}</h2>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
              {recentListings.map((listing) => (
                <ListingCard key={listing.id} listing={listing} />
              ))}
            </div>
          </div>
          <div className="hidden lg:block">
            <Sidebar />
          </div>
        </div>
      </div>
    </>
  )
}
```

- [ ] **Step 3: app/[locale]/category/page.tsx を作成**

```typescript
// app/[locale]/category/page.tsx
import { getTranslations } from 'next-intl/server'
import { Link } from '@/i18n/navigation'
import Sidebar from '@/components/Sidebar'
import gamesData from '@/data/games.json'

export default async function CategoryPage({
  searchParams,
}: {
  searchParams: Promise<{ type?: string | string[] }>
}) {
  const t = await getTranslations('category')
  const params = await searchParams
  const rawType = Array.isArray(params.type) ? params.type[0] : params.type
  const activeType = rawType && ['pc', 'mobile', 'other'].includes(rawType) ? rawType : undefined

  const CATEGORY_LABELS: Record<string, string> = {
    pc: t('pc'),
    mobile: t('mobile'),
    other: t('other'),
  }

  const categories = activeType ? [activeType] : ['pc', 'mobile', 'other']
  const filteredGames = activeType
    ? gamesData.filter((g) => g.category === activeType)
    : gamesData

  return (
    <div className="max-w-screen-xl mx-auto px-4 py-6">
      <div className="flex gap-6">
        <div className="flex-1 min-w-0">
          <h1 className="text-lg font-bold text-gray-800 mb-6">{t('title')}</h1>
          {categories.map((cat) => {
            const games = filteredGames.filter((g) => g.category === cat)
            if (games.length === 0) return null
            return (
              <div key={cat} className="mb-8">
                <h2 className="text-sm font-bold text-blue-600 border-b border-blue-200 pb-1 mb-3">
                  {CATEGORY_LABELS[cat] ?? cat}
                </h2>
                <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-3">
                  {games.map((game) => (
                    <Link
                      key={game.id}
                      href={`/game/${game.slug}`}
                      className="block bg-blue-50 hover:bg-blue-100 border border-blue-100 rounded px-3 py-2 text-sm text-center text-blue-700 hover:text-blue-900 transition-colors"
                    >
                      {game.name}
                    </Link>
                  ))}
                </div>
              </div>
            )
          })}
        </div>
        <div className="hidden lg:block">
          <Sidebar />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 古いファイルを削除**

```bash
rm "C:/Users/unitu/RMTsite/app/layout.tsx"
rm "C:/Users/unitu/RMTsite/app/page.tsx"
rm -rf "C:/Users/unitu/RMTsite/app/category"
```

- [ ] **Step 5: コミット**

```bash
git add app/
git commit -m "feat: app/ を app/[locale]/ に再構成"
```

---

## Task 6: LanguageSwitcher コンポーネントを作成

**Files:**
- Create: `components/LanguageSwitcher.tsx`
- Create: `__tests__/LanguageSwitcher.test.tsx`

- [ ] **Step 1: テストを書く**

`__tests__/LanguageSwitcher.test.tsx`:

```typescript
import { render, screen } from '@testing-library/react'
import { NextIntlClientProvider } from 'next-intl'
import LanguageSwitcher from '@/components/LanguageSwitcher'

// next-intl/navigation をモック
jest.mock('@/i18n/navigation', () => ({
  usePathname: () => '/',
  useRouter: () => ({ replace: jest.fn() }),
}))

function renderWithIntl(locale: string) {
  return render(
    <NextIntlClientProvider locale={locale} messages={{}}>
      <LanguageSwitcher />
    </NextIntlClientProvider>
  )
}

describe('LanguageSwitcher', () => {
  it('EN / JA / ZH の3ボタンを表示する', () => {
    renderWithIntl('en')
    expect(screen.getByText('EN')).toBeInTheDocument()
    expect(screen.getByText('JA')).toBeInTheDocument()
    expect(screen.getByText('ZH')).toBeInTheDocument()
  })

  it('現在のロケール（en）のボタンがハイライトされる', () => {
    renderWithIntl('en')
    const enButton = screen.getByText('EN')
    expect(enButton).toHaveClass('bg-white')
  })

  it('現在のロケール（ja）のボタンがハイライトされる', () => {
    renderWithIntl('ja')
    const jaButton = screen.getByText('JA')
    expect(jaButton).toHaveClass('bg-white')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx jest __tests__/LanguageSwitcher.test.tsx
```

期待出力: FAIL

- [ ] **Step 3: LanguageSwitcher を実装**

`components/LanguageSwitcher.tsx`:

```typescript
'use client'

import { useLocale } from 'next-intl'
import { useRouter, usePathname } from '@/i18n/navigation'

const LOCALES = [
  { code: 'en', label: 'EN' },
  { code: 'ja', label: 'JA' },
  { code: 'zh', label: 'ZH' },
] as const

export default function LanguageSwitcher() {
  const locale = useLocale()
  const router = useRouter()
  const pathname = usePathname()

  const switchLocale = (newLocale: string) => {
    router.replace(pathname, { locale: newLocale })
  }

  return (
    <div className="flex gap-1 text-xs">
      {LOCALES.map(({ code, label }) => (
        <button
          key={code}
          onClick={() => switchLocale(code)}
          className={
            code === locale
              ? 'bg-white text-blue-600 px-2 py-1 rounded font-bold'
              : 'text-white/80 hover:text-white px-2 py-1 rounded'
          }
        >
          {label}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npx jest __tests__/LanguageSwitcher.test.tsx
```

期待出力: PASS（3 tests passed）

- [ ] **Step 5: コミット**

```bash
git add components/LanguageSwitcher.tsx __tests__/LanguageSwitcher.test.tsx
git commit -m "feat: LanguageSwitcher コンポーネントを追加"
```

---

## Task 7: Header を翻訳対応に更新

**Files:**
- Modify: `components/Header.tsx`
- Modify: `__tests__/Header.test.tsx`

- [ ] **Step 1: __tests__/Header.test.tsx を更新**

```typescript
import { render, screen } from '@testing-library/react'
import { NextIntlClientProvider } from 'next-intl'

// next-intl/server をモック（Server Component のため）
jest.mock('next-intl/server', () => ({
  getTranslations: jest.fn().mockResolvedValue((key: string) => {
    const en: Record<string, string | Record<string, string>> = {
      search: 'Search game titles',
      login: 'Login',
      register: 'Sign Up',
      nav: { pc: 'PC Games', mobile: 'Mobile Games', other: 'Other', all: 'All Titles' },
    }
    const val = en[key]
    if (typeof val === 'string') return val
    return (subkey: string) => (val as Record<string, string>)[subkey] ?? subkey
  }),
}))

// LanguageSwitcher をモック（Client Component で next-intl/navigation が必要なため）
jest.mock('@/components/LanguageSwitcher', () => ({
  __esModule: true,
  default: () => <div data-testid="language-switcher" />,
}))

jest.mock('@/i18n/navigation', () => ({
  Link: ({ href, children, className }: { href: string; children: React.ReactNode; className?: string }) => (
    <a href={href} className={className}>{children}</a>
  ),
}))

import Header from '@/components/Header'

describe('Header', () => {
  it('ロゴテキストを表示する', async () => {
    const header = await Header()
    render(
      <NextIntlClientProvider locale="en" messages={{}}>
        {header}
      </NextIntlClientProvider>
    )
    expect(screen.getByText('RMTsite')).toBeInTheDocument()
  })

  it('検索バーを表示する', async () => {
    const header = await Header()
    render(
      <NextIntlClientProvider locale="en" messages={{}}>
        {header}
      </NextIntlClientProvider>
    )
    expect(screen.getByPlaceholderText('Search game titles')).toBeInTheDocument()
  })

  it('ログインリンクを表示する', async () => {
    const header = await Header()
    render(
      <NextIntlClientProvider locale="en" messages={{}}>
        {header}
      </NextIntlClientProvider>
    )
    expect(screen.getByText('Login')).toBeInTheDocument()
  })

  it('会員登録リンクを表示する', async () => {
    const header = await Header()
    render(
      <NextIntlClientProvider locale="en" messages={{}}>
        {header}
      </NextIntlClientProvider>
    )
    expect(screen.getByText('Sign Up')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx jest __tests__/Header.test.tsx
```

期待: FAIL（'Search game titles' が見つからない）

- [ ] **Step 3: Header.tsx を更新**

```typescript
// components/Header.tsx
import { getTranslations } from 'next-intl/server'
import { Link } from '@/i18n/navigation'
import LanguageSwitcher from '@/components/LanguageSwitcher'

export default async function Header() {
  const t = await getTranslations('header')

  return (
    <header>
      <div className="bg-blue-600 text-white">
        <div className="max-w-screen-xl mx-auto px-4 py-2 flex items-center gap-4">
          <Link href="/" className="text-xl font-bold whitespace-nowrap">
            RMTsite
          </Link>
          <form action="/search" method="get" className="flex-1">
            <input
              type="text"
              name="q"
              placeholder={t('search')}
              className="w-full px-3 py-1.5 text-sm text-gray-800 rounded border-0 outline-none"
              aria-label={t('search')}
            />
          </form>
          <div className="flex items-center gap-3 text-sm whitespace-nowrap">
            <Link href="/login" className="hover:underline">{t('login')}</Link>
            <Link href="/register" className="hover:underline">{t('register')}</Link>
            <LanguageSwitcher />
          </div>
        </div>
      </div>
      <nav className="bg-blue-50 border-b border-blue-100">
        <div className="max-w-screen-xl mx-auto px-4 py-1.5 flex gap-6 text-sm text-blue-700">
          <Link href="/category?type=pc" className="hover:underline">{t('nav.pc')}</Link>
          <Link href="/category?type=mobile" className="hover:underline">{t('nav.mobile')}</Link>
          <Link href="/category?type=other" className="hover:underline">{t('nav.other')}</Link>
          <Link href="/category" className="hover:underline">{t('nav.all')}</Link>
        </div>
      </nav>
    </header>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npx jest __tests__/Header.test.tsx
```

期待出力: PASS（4 tests passed）

- [ ] **Step 5: コミット**

```bash
git add components/Header.tsx __tests__/Header.test.tsx
git commit -m "feat: Header を多言語対応に更新"
```

---

## Task 8: Footer を翻訳対応に更新

**Files:**
- Modify: `components/Footer.tsx`
- Modify: `__tests__/Footer.test.tsx`

- [ ] **Step 1: __tests__/Footer.test.tsx を更新**

```typescript
import { render, screen } from '@testing-library/react'

jest.mock('next-intl/server', () => ({
  getTranslations: jest.fn().mockResolvedValue((key: string) => {
    const en: Record<string, string> = {
      terms: 'Terms of Service',
      privacy: 'Privacy Policy',
      contact: 'Contact Us',
      guide: 'User Guide',
    }
    return en[key] ?? key
  }),
}))

jest.mock('@/i18n/navigation', () => ({
  Link: ({ href, children, className }: { href: string; children: React.ReactNode; className?: string }) => (
    <a href={href} className={className}>{children}</a>
  ),
}))

import Footer from '@/components/Footer'

describe('Footer', () => {
  it('Terms of Service リンクを表示する', async () => {
    const footer = await Footer()
    render(footer)
    expect(screen.getByText('Terms of Service')).toBeInTheDocument()
  })

  it('Privacy Policy リンクを表示する', async () => {
    const footer = await Footer()
    render(footer)
    expect(screen.getByText('Privacy Policy')).toBeInTheDocument()
  })

  it('コピーライトを表示する', async () => {
    const footer = await Footer()
    render(footer)
    expect(screen.getByText(/RMTsite/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx jest __tests__/Footer.test.tsx
```

- [ ] **Step 3: Footer.tsx を更新**

```typescript
// components/Footer.tsx
import { getTranslations } from 'next-intl/server'
import { Link } from '@/i18n/navigation'

export default async function Footer() {
  const t = await getTranslations('footer')

  return (
    <footer className="bg-gray-800 text-gray-400 text-sm mt-8">
      <div className="max-w-screen-xl mx-auto px-4 py-6 flex flex-col gap-3">
        <div className="flex flex-wrap gap-4">
          <Link href="/terms" className="hover:text-white">{t('terms')}</Link>
          <Link href="/privacy" className="hover:text-white">{t('privacy')}</Link>
          <Link href="/contact" className="hover:text-white">{t('contact')}</Link>
          <Link href="/guide" className="hover:text-white">{t('guide')}</Link>
        </div>
        <p className="text-xs text-gray-500">© 2026 RMTsite All Rights Reserved.</p>
      </div>
    </footer>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npx jest __tests__/Footer.test.tsx
```

期待出力: PASS（3 tests passed）

- [ ] **Step 5: コミット**

```bash
git add components/Footer.tsx __tests__/Footer.test.tsx
git commit -m "feat: Footer を多言語対応に更新"
```

---

## Task 9: Carousel を翻訳対応に更新

**Files:**
- Modify: `components/Carousel.tsx`

（Carousel は `"use client"` コンポーネントなのでテスト更新不要。テストはスキップ済み）

- [ ] **Step 1: Carousel.tsx を更新**

```typescript
// components/Carousel.tsx
'use client'

import useEmblaCarousel from 'embla-carousel-react'
import Autoplay from 'embla-carousel-autoplay'
import { useRef } from 'react'
import { useTranslations } from 'next-intl'

export default function Carousel() {
  const t = useTranslations('carousel')
  const autoplay = useRef(Autoplay({ delay: 4000, stopOnInteraction: false }))
  const [emblaRef] = useEmblaCarousel({ loop: true }, [autoplay.current])

  const BANNERS = [
    { id: 1, text: t('slide1'), bg: 'bg-blue-600' },
    { id: 2, text: t('slide2'), bg: 'bg-blue-700' },
    { id: 3, text: t('slide3'), bg: 'bg-blue-800' },
  ]

  return (
    <div className="overflow-hidden" ref={emblaRef}>
      <div className="flex">
        {BANNERS.map((banner) => (
          <div
            key={banner.id}
            className={`flex-none w-full ${banner.bg} text-white py-12 text-center text-lg font-bold`}
          >
            {banner.text}
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: ビルドが通ることを確認**

```bash
npm run build
```

- [ ] **Step 3: コミット**

```bash
git add components/Carousel.tsx
git commit -m "feat: Carousel を多言語対応に更新"
```

---

## Task 10: Sidebar を翻訳対応に更新

**Files:**
- Modify: `components/Sidebar.tsx`
- Modify: `__tests__/Sidebar.test.tsx`

- [ ] **Step 1: __tests__/Sidebar.test.tsx を更新**

```typescript
import { render, screen } from '@testing-library/react'

jest.mock('next-intl/server', () => ({
  getTranslations: jest.fn().mockResolvedValue((key: string) => {
    const en: Record<string, string> = {
      categoryTitle: 'Categories',
      searchTitle: 'Search Titles',
      searchPlaceholder: 'Enter game name',
    }
    return en[key] ?? key
  }),
}))

jest.mock('@/i18n/navigation', () => ({
  Link: ({ href, children, className }: { href: string; children: React.ReactNode; className?: string }) => (
    <a href={href} className={className}>{children}</a>
  ),
}))

// category の翻訳も必要（サイドバーがカテゴリ名を直接書いている場合）
// Sidebar は sidebar 名前空間のみ使うので category 翻訳は不要

import Sidebar from '@/components/Sidebar'

describe('Sidebar', () => {
  it('カテゴリリンクを表示する', async () => {
    const sidebar = await Sidebar()
    render(sidebar)
    expect(screen.getByText(/PC Games/)).toBeInTheDocument()
    expect(screen.getByText(/Mobile Games/)).toBeInTheDocument()
    expect(screen.getByText(/Other/)).toBeInTheDocument()
  })

  it('タイトル検索フォームを表示する', async () => {
    const sidebar = await Sidebar()
    render(sidebar)
    expect(screen.getByPlaceholderText('Enter game name')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx jest __tests__/Sidebar.test.tsx
```

- [ ] **Step 3: Sidebar.tsx を更新**

```typescript
// components/Sidebar.tsx
import { getTranslations } from 'next-intl/server'
import { Link } from '@/i18n/navigation'

export default async function Sidebar() {
  const tSidebar = await getTranslations('sidebar')
  const tCategory = await getTranslations('category')

  return (
    <aside className="w-48 shrink-0">
      <div className="bg-gray-50 border border-gray-200 rounded p-3 mb-4">
        <h3 className="text-sm font-bold text-gray-700 mb-2">{tSidebar('categoryTitle')}</h3>
        <ul className="space-y-1 text-sm">
          <li>
            <Link href="/category?type=pc" className="text-blue-600 hover:underline flex items-center gap-1">
              ▶ {tCategory('pc')}
            </Link>
          </li>
          <li>
            <Link href="/category?type=mobile" className="text-blue-600 hover:underline flex items-center gap-1">
              ▶ {tCategory('mobile')}
            </Link>
          </li>
          <li>
            <Link href="/category?type=other" className="text-blue-600 hover:underline flex items-center gap-1">
              ▶ {tCategory('other')}
            </Link>
          </li>
        </ul>
      </div>
      <div className="bg-gray-50 border border-gray-200 rounded p-3">
        <h3 className="text-sm font-bold text-gray-700 mb-2">{tSidebar('searchTitle')}</h3>
        <input
          type="text"
          placeholder={tSidebar('searchPlaceholder')}
          className="w-full px-2 py-1.5 text-sm border border-gray-300 rounded outline-none focus:border-blue-500"
        />
      </div>
    </aside>
  )
}
```

Note: Sidebar test の `getTranslations` mock は `(namespace)` 引数を受け取るように更新が必要。

Sidebar テストの mock を以下に修正:

```typescript
jest.mock('next-intl/server', () => ({
  getTranslations: jest.fn().mockImplementation((namespace: string) => {
    const translations: Record<string, Record<string, string>> = {
      sidebar: {
        categoryTitle: 'Categories',
        searchTitle: 'Search Titles',
        searchPlaceholder: 'Enter game name',
      },
      category: {
        pc: 'PC Games',
        mobile: 'Mobile Games',
        other: 'Other',
      },
    }
    return Promise.resolve((key: string) => translations[namespace]?.[key] ?? key)
  }),
}))
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npx jest __tests__/Sidebar.test.tsx
```

期待出力: PASS（2 tests passed）

- [ ] **Step 5: コミット**

```bash
git add components/Sidebar.tsx __tests__/Sidebar.test.tsx
git commit -m "feat: Sidebar を多言語対応に更新"
```

---

## Task 11: GameRankingTable に listingUnit prop を追加

**Files:**
- Modify: `components/GameRankingTable.tsx`
- Modify: `__tests__/GameRankingTable.test.tsx`

- [ ] **Step 1: __tests__/GameRankingTable.test.tsx を更新**

```typescript
import { render, screen } from '@testing-library/react'
import GameRankingTable from '@/components/GameRankingTable'

const mockGames = [
  { id: 1, name: 'Game A', slug: 'game-a', category: 'pc' as const, listingCount: 100000 },
  { id: 2, name: 'Game B', slug: 'game-b', category: 'mobile' as const, listingCount: 200000 },
]

describe('GameRankingTable', () => {
  it('タイトルを表示する', () => {
    render(<GameRankingTable games={mockGames} title="Mobile Games Ranking" listingUnit="listings" />)
    expect(screen.getByText('Mobile Games Ranking')).toBeInTheDocument()
  })

  it('ゲーム名を表示する', () => {
    render(<GameRankingTable games={mockGames} title="Ranking" listingUnit="listings" />)
    expect(screen.getByText('Game A')).toBeInTheDocument()
    expect(screen.getByText('Game B')).toBeInTheDocument()
  })

  it('出品件数を表示する（EN: listings）', () => {
    render(<GameRankingTable games={mockGames} title="Ranking" listingUnit="listings" />)
    expect(screen.getByText('100,000 listings')).toBeInTheDocument()
    expect(screen.getByText('200,000 listings')).toBeInTheDocument()
  })

  it('出品件数を表示する（JA: 件）', () => {
    render(<GameRankingTable games={mockGames} title="Ranking" listingUnit="件" />)
    expect(screen.getByText('100,000件')).toBeInTheDocument()
    expect(screen.getByText('200,000件')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx jest __tests__/GameRankingTable.test.tsx
```

- [ ] **Step 3: GameRankingTable.tsx を更新**

```typescript
// components/GameRankingTable.tsx
import { Link } from '@/i18n/navigation'
import { Game } from '@/types'

type Props = {
  games: Game[]
  title: string
  listingUnit: string
}

export default function GameRankingTable({ games, title, listingUnit }: Props) {
  return (
    <div className="bg-white border border-gray-200 rounded">
      <div className="bg-blue-600 text-white px-4 py-2 text-sm font-bold rounded-t">
        {title}
      </div>
      <table className="w-full text-sm">
        <tbody>
          {games.map((game, index) => (
            <tr key={game.id} className="border-b border-gray-100 hover:bg-gray-50">
              <td className="px-3 py-2 text-gray-500 w-8">{index + 1}</td>
              <td className="px-3 py-2">
                <Link href={`/game/${game.slug}`} className="text-blue-600 hover:underline">
                  {game.name}
                </Link>
              </td>
              <td className="px-3 py-2 text-right text-gray-500">
                {game.listingCount.toLocaleString('ja-JP')}{listingUnit === '件' ? '件' : ` ${listingUnit}`}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npx jest __tests__/GameRankingTable.test.tsx
```

期待出力: PASS（4 tests passed）

- [ ] **Step 5: コミット**

```bash
git add components/GameRankingTable.tsx __tests__/GameRankingTable.test.tsx
git commit -m "feat: GameRankingTable に listingUnit prop を追加"
```

---

## Task 12: 全テスト実行・ビルド確認

**Files:** なし（確認のみ）

- [ ] **Step 1: 全テストを実行**

```bash
npx jest
```

期待出力: 全スイート PASS（Header: 4, Footer: 3, GameRankingTable: 4, ListingCard: 3, Sidebar: 2, LanguageSwitcher: 3 = 合計 19 tests）

失敗するテストがある場合は、エラーメッセージを確認してモックまたは翻訳テキストを修正する。

- [ ] **Step 2: ビルドが通ることを確認**

```bash
npm run build
```

期待出力: `✓ Compiled successfully`

ビルドが失敗する場合の主なチェックポイント:
- `app/[locale]/layout.tsx` の `params` が `Promise<{locale: string}>` になっているか（Next.js 14 では非 Promise の場合もある — エラーを見て調整）
- `i18n/request.ts` の import パスが正しいか
- `messages/` ディレクトリが存在するか

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```

- `http://localhost:3000` → `http://localhost:3000/en` にリダイレクトされることを確認
- `http://localhost:3000/en` → 英語のトップページが表示されることを確認
- `http://localhost:3000/ja` → 日本語のトップページが表示されることを確認
- `http://localhost:3000/zh` → 中国語のトップページが表示されることを確認
- ヘッダー右上の EN/JA/ZH ボタンで言語が切り替わることを確認

- [ ] **Step 4: 最終コミット**

```bash
git add .
git commit -m "chore: i18n 実装完了 - EN/JA/ZH 多言語対応"
```
